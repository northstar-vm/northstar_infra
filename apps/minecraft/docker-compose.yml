services:
  minecraft:
    image: itzg/minecraft-server:java25
    container_name: northstar-minecraft
    restart: "no"
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      TYPE: "PAPER"
      VERSION: "${MINECRAFT_VERSION:-LATEST}"
      MEMORY: "${MEMORY:-12G}"
      MOTD: "${SERVER_NAME:-Northstar}"
      MODE: "survival"
      DIFFICULTY: "normal"
      ONLINE_MODE: "FALSE"
      ENFORCE_SECURE_PROFILE: "FALSE"
      CREATE_CONSOLE_IN_PIPE: "TRUE"
      OPS: "${OPS:-}"
      WHITELIST: "${WHITELIST:-}"
      UID: "${MINECRAFT_UID:-1000}"
      GID: "${MINECRAFT_GID:-1000}"
    volumes:
      - /opt/northstar/apps/minecraft/data:/data
    networks:
      - northstar_web

networks:
  northstar_web:
    external: true
